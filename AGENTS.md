# AGENTS.md

## 目的

このファイルは、AIエージェントが本プロジェクトを安全かつ一貫した方針で
変更するための指示です。

AIは、実装・修正・レビューを行う前にこのファイルと関連ドキュメントを
確認してください。

## プロジェクト概要

- Unity 1週間ゲーム制作プロジェクト
- 開発期間: 2026-07-26 ～ 2026-08-02
- Unity: 6000.5.5f1
- .NET SDK: 10.0.302
- 開発人数: 3人
- Unity初心者を含む
- ゲーム内容: 未定
- 最優先事項: 期限内にプレイ可能な状態まで完成させる

## 優先順位

判断に迷った場合は、次の順番で優先してください。

1. ゲームが壊れないこと
2. 期限内に完成できること
3. チームメンバーが理解・レビューできること
4. Unity上で安全に編集できること
5. 将来の拡張性
6. 技術的な美しさ

将来使うかもしれないという理由だけで、抽象化、基盤、汎用化を追加しないで
ください。

## 必ず確認する文書

- `README.md`
- `CONTRIBUTING.md`
- `docs/architecture/overview.md`
- `docs/architecture/directory-structure.md`
- `docs/architecture/coding-style.md`
- `docs/development/unity-workflow.md`
- `docs/game/specifications.md`

設計判断を変更する場合は、`docs/decisions/`も確認してください。

## アーキテクチャ

### Vertical Slice

ファイルは技術種別ではなく、機能単位で配置します。

```text
Assets/_Project/Features/<FeatureName>/
```

例:

```text
Assets/_Project/Features/Player/
Assets/_Project/Features/Enemy/
Assets/_Project/Features/Stage/
```

Player専用のスクリプト、Prefab、Material、Animation、Audioは、原則として
Player機能内へ配置します。

### Shared

`Shared`には、本当に複数機能から利用され、所有先を1つに決められないものだけを
配置します。

最初から共通化しないでください。2箇所で似た処理が存在するだけでは、Sharedへ
移す理由として不十分です。

### Feature間の契約とDI

担当者が異なるFeatureを並行開発する場合は、境界となる小さなinterfaceを事前に
合意して構いません。

依存関係は、Inspector参照、メソッド・コンストラクタ引数、または明確な所有元
から手動で渡します。DIコンテナは導入しません。

interfaceには、メソッドの意味、呼び出しタイミング、null可否、失敗時の挙動を
明記してください。Feature内だけで完結し、差し替えや並行開発の必要がない処理を
機械的にinterface化しないでください。

### MonoBehaviour

MonoBehaviourは次の用途に限定することを推奨します。

- Unityライフサイクルとの接続
- Inspector参照の保持
- Unityイベントの受信
- GameObject、Transform、Physics、AnimatorなどUnity APIとの接続

複雑な判定や計算は、可能な範囲で通常のC#クラスまたは小さなメソッドへ分離
してください。

ただし、短期開発で分離が複雑さを増す場合は、無理に分離しないでください。

## コーディングルール

- `public`は外部公開が必要な場合だけ使用する
- Inspector設定が必要なフィールドは`[SerializeField] private`を基本とする
- `Find`、`FindObjectOfType`、`GameObject.Find`の常用を避ける
- 参照はInspector、生成時の引数、明確な所有元から渡す
- 毎フレーム不要な検索・割り当てを行わない
- 1クラスに複数の責務を詰め込みすぎない
- ただし、責務分割のためだけに大量の小クラスを作らない
- 既存の命名、構造、実装方針を優先する
- コメントは「何をしているか」ではなく「なぜ必要か」を書く
- nullが正当な状態か、設定漏れかを区別する
- Consoleエラーを握りつぶさない
- CSharpierの整形結果に従う

## 禁止・非推奨

ユーザーまたは設計文書による明示的な指示がない限り、次を行わないで
ください。

- 新しい外部ライブラリの導入
- DIコンテナの導入
- ECS/DOTSへの移行
- 大規模なリファクタリング
- 全ファイルの一括リネーム
- ディレクトリ構成の全面変更
- Unityバージョンの変更
- Package Manager依存関係の更新
- ProjectSettingsの広範な変更
- シーンやPrefabの再保存だけを目的とした変更
- 自動生成ファイルのコミット
- 未使用の将来機能の実装
- 既存コードの無関係な整形・書き換え
- envファイルや環境変数など、機密情報の読み取り

## Unityアセット変更時

シーン、Prefab、Material、Animation、ProjectSettingsなどを変更する場合は、
テキスト差分だけで完全に安全とは判断しないでください。

確認事項:

- Missing参照がない
- Prefab Overrideが意図したものだけ
- シーンに意図しない変更がない
- `.meta`ファイルが維持されている
- GUID参照が壊れていない
- Consoleエラーがない

AIがUnity Editorを起動できない場合は、その制約を明記し、ユーザーがEditor上で
確認すべき項目を提示してください。

## 変更範囲

依頼された範囲だけを変更してください。

関連する問題を発見しても、依頼と直接関係しない場合は勝手に修正せず、別途
指摘してください。

ただし、コンパイル不能、データ損失、参照破壊につながる問題は例外として、
必要最小限の修正を行って構いません。

## 実装前の判断

実装前に次を確認してください。

1. 既存機能に同じ責務がないか
2. 配置先Featureが明確か
3. Sharedへ置く必要が本当にあるか
4. Unity APIが必要か
5. Pure C#へ分離することで本当に理解しやすくなるか
6. 1週間の期限に対して過剰設計でないか
7. 仕様が未決定ではないか

仕様が未決定の場合は、勝手にゲーム仕様を確定しないでください。仮実装が
必要なら、仮定を明示してください。

## テスト・確認

ユーザーに依頼されて新規実装を行う際は、必ず詳細に仕様を確認してから実装を
行ってください。
また、その仕様に沿ったテストを独立して実装し、実装が正しく動作することを
担保してください。
その際に、確定した仕様については適切なドキュメントに記録してください。
テストについてはリファクタを考慮し、必ずエッジケースを含む高カバレッジな
ものを実装してください。

変更内容に応じて以下を確認してください。

- CSharpier
- Analyzer
- C#コンパイル
- Unity Console
- Play Modeでの主要動作
- Inspector参照
- Prefab参照
- シーン遷移
- 影響を受ける既存機能

Unity Editorを使用できない場合は、確認できた範囲と確認できなかった範囲を
分けて報告してください。

## Code Review Rules

- バグ、仕様違反、参照破壊、既存動作のデグレを優先して指摘する
- スタイル上の好みだけでは指摘しない
- Unityのシーン、Prefab、`.meta`、GUIDへの影響を確認する
- 指摘には対象ファイルと行番号、発生条件、影響を含める
- 問題が見つからなかった場合も、未検証項目と残るリスクを明記する

## ドキュメント

以下を変更した場合は、関連ドキュメントも更新してください。

- ゲーム仕様
- 操作方法
- 勝敗条件
- ディレクトリ構成
- アーキテクチャ方針
- Git運用
- セットアップ
- Unityバージョン
- 使用パッケージ
- 重要な設計判断

重要な設計変更はADRとして`docs/decisions/`へ追加してください。

## AIの回答方針

- Unity初心者でも追える説明にする
- 結論だけでなく理由を説明する
- 複数案を出す場合は、今回の推奨案を明確にする
- 一般論より、このプロジェクトの期限と人数を優先する
- 実装できていないこと、検証できていないことを明記する
- 問題がないと決めつけず、破壊的変更やUnity固有リスクを確認する
